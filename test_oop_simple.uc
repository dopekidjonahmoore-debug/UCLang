class Player {
    x == 0.
    y == 0.

    def init(x, y.
        this.x == x.
        this.y == y.
    )

    def getX(.
        response(this.x).
    )
}

p == new Player(10, 20.).
print("x = ").
print(p.x).
print("\n").
print("y = ").
print(p.y).
print("\n").
